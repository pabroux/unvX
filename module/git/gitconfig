[color]
	diff = auto
	status = auto
	branch = auto
[user]
	name = pabroux
	email = pierre-alexandrebroux@live.fr
[alias]
	br = branch
	ci = commit
	co = checkout
	lg = log
 	ph = push
	pl = pull
	re = restore
	reu = restore --staged
	rt = reset
	sh = stash
	st = status
	sw = switch
[push]
	default = matching
[http]
	sslVerify = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
